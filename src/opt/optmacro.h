#pragma once

/**
 * @brief Enable safe initialization for undefined local variables.
 * This will disable some optimizations, but will make sure that
 * all local variables are safely initialized.
 * 
*/
#define ENABLE_SAFE_INIT

